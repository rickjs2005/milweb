# 05 — Internacionalização PT-BR / EN / ES

Data: 28/08/2026 · Branch `main`

## Arquitetura

- **Segmento interno** `app/[lang]/…` (pt/en/es) reaproveitado; as pastas internas continuam `work`, `lab`, `studio`, `services`, `contact`, `diagnostico` e os slugs de serviço em PT.
- **URLs públicas localizadas** via tabela única `src/i18n/routing.ts` (`ROUTES`): o middleware traduz público → interno (`internalizePath`) e `localizePath`/`withLocale` fazem o inverso em links, canonical, hreflang e sitemap. Nenhuma página duplicada.
- **URLs antigas / cruzadas** (`/work`, `/studio`, `/services`, `/contact`, `/en/diagnostico`, `/en/criacao-de-sites`, `/es/work`, `/es/projetos/x`, `/pt/...`) → **308 direto** para a canônica, sem cadeia. `/raio-x` e `/en/raio-x` seguem em `next.config.mjs`.
- **`<html lang>` do servidor**: `app/layout.tsx` é só passagem; quem renderiza `<html lang="pt-BR|en|es">` é `app/[lang]/layout.tsx` (React 19 trata html/body como singletons, então o documento pode vir de um layout aninhado). Fontes compartilhadas em `lib/fonts.ts`.
- **404**: o middleware valida a URL contra a tabela de rotas + `src/data/slugs.ts` (lista leve dos 25 slugs, conferida contra `PROJECTS` em `data/projects.ts` — divergência derruba o build), anexa o header `x-milweb-locale` e reescreve rota desconhecida para um caminho interno sem página → o Next responde com o **404 global `app/not-found.tsx`**, que lê o idioma do header e renderiza o documento inteiro (fontes, CSS, `lang` certo, `noindex`, status 404 real). Dois becos sem saída que motivaram isso: `notFound()` sob o layout raiz dinâmico só manda o shell genérico `<html id="__next_error__">` no servidor (o layout real aparece só após hidratar) e a Vercel intercepta rewrite de middleware com `status: 404` e serve o 404 estático dela.
- **Dicionários tipados** `src/i18n/{types,pt,en,es}.ts` (`Dictionary`, `getDict`). `Localized = { pt; en; es }` — `es` obrigatório em todo conteúdo (`content.ts`, `content-projects-*.ts`, `services.ts`, `case-stories.ts`, `case-extras.ts`, `studio.ts`, `work.ts`). Chave faltando = erro de TypeScript = build quebrado. Sem `any`.
- **Dados de projeto** continuam fonte única (`data/projects.ts`: ordem, numeração, total derivado, `displayType`, cadeia next). Só o texto é localizado; rótulos de UI (`displayType`, filtros, colunas, capítulos do case, peças do Aurex, "DIA n / 132") vêm do dicionário.
- **Seletor `PT EN ES`** (`components/nav/language-switch.tsx`): `<nav aria-label>` + links com `hreflang`/`lang`, `aria-current="page"` no ativo (ponto signal), alvo ≥44px, foco visível, no header desktop e no menu mobile. A troca mantém a página equivalente (`alternatesOf`). Grava cookie `milweb_locale` (1 ano); **só a raiz exata `/`** honra o cookie (307 para `/en` ou `/es`); links diretos sempre respeitam a URL. Sem modal, sem Accept-Language.
- **SEO**: `lib/seo.ts#pageMetadata` → canonical na URL do idioma + hreflang `pt-BR`/`en`/`es`/`x-default` (= PT) + `og:locale` (`pt_BR`/`en_US`/`es_419`) + title/description traduzidos. Sitemap trilíngue (111 URLs, 37 por idioma) com alternates. JSON-LD com `inLanguage` por idioma. OG/Twitter images aceitam `es`.
- **Tipografia em três idiomas**: `.t-fit` / `.t-fit-md` (globals.css) + `lib/fit.ts#fitLines` escalam a headline pela linha mais longa (`--chars`) — a headline ES do hero cabe no ultrawide e "EMPEZAR UN PROYECTO." cabe em 320px sem quebrar palavra. Overlay mobile em `100dvh` (seletor não fica sob a barra do navegador).

## Tabela de rotas

| Interno | PT | EN | ES |
|---|---|---|---|
| `/` | `/` | `/en` | `/es` |
| `/work` | `/projetos` | `/en/work` | `/es/proyectos` |
| `/work/[slug]` | `/projetos/[slug]` | `/en/work/[slug]` | `/es/proyectos/[slug]` |
| `/lab` | `/lab` | `/en/lab` | `/es/lab` |
| `/studio` | `/estudio` | `/en/studio` | `/es/estudio` |
| `/services` | `/servicos` | `/en/services` | `/es/servicios` |
| `/contact` | `/contato` | `/en/contact` | `/es/contacto` |
| `/diagnostico` | `/diagnostico` | `/en/business-audit` | `/es/diagnostico` |
| `/criacao-de-sites` | `/criacao-de-sites` | `/en/website-development` | `/es/desarrollo-web` |
| `/catalogo-whatsapp` | `/catalogo-whatsapp` | `/en/whatsapp-catalog` | `/es/catalogo-whatsapp` |
| `/landing-pages` | `/landing-pages` | `/en/landing-pages` | `/es/landing-pages` |
| `/loja-virtual` | `/loja-virtual` | `/en/online-store` | `/es/tienda-online` |
| `/sistemas-sob-medida` | `/sistemas-sob-medida` | `/en/custom-systems` | `/es/sistemas-a-medida` |

Todas 200 no build de produção local; canonical = própria URL; hreflang com as três + x-default.

Redirects 308 conferidos (um salto): `/work→/projetos`, `/work/terral→/projetos/terral`, `/studio→/estudio`, `/services→/servicos`, `/contact→/contato`, `/en/projetos→/en/work`, `/en/projetos/terral→/en/work/terral`, `/en/diagnostico→/en/business-audit`, `/en/criacao-de-sites→/en/website-development`, `/en/catalogo-whatsapp→/en/whatsapp-catalog`, `/en/loja-virtual→/en/online-store`, `/en/sistemas-sob-medida→/en/custom-systems`, `/es/work→/es/proyectos`, `/es/services→/es/servicios`, `/es/projetos/terral→/es/proyectos/terral`, `/pt→/`, `/pt/work/terral→/projetos/terral`, `/raio-x→/diagnostico`, `/en/raio-x→/en/business-audit`.

## Validação (build de produção local, `next start`)

- `pnpm tsc --noEmit` limpo · `pnpm lint` limpo (1 warning pré-existente do `<img>` no Satori) · `pnpm build` verde.
- `.audit/routes.sh`: 40 rotas PT/EN/ES 200; 19 legadas 308 diretas; cookie `es` em `/` → 307 `/es`; `/projetos` e `/es/lab` com cookie divergente → 200 (URL manda); 404 reais.
- `node scripts/i18n-audit.mjs <base>`: lê o sitemap, baixa as 111 URLs + 3 de 404, extrai texto visível/alt/aria/title/description/OG, procura palavras proibidas por idioma (allowlist de nomes próprios e termos técnicos), confere `<html lang>`, canonical, hreflang, x-default, `og:locale`, JSON-LD `inLanguage`. **114 rotas · 0 problemas.** Vazamentos que a auditoria pegou e foram corrigidos: `TYPE →` nas capacidades, "SERVICES." / "START A PROJECT." em PT/ES, `Studio — creative development` no title PT/ES, "Partículas" no `/en/lab`, rótulos ingleses dos capítulos/fluxo/palavras dos 4 cases (CATALOG, THE SECRET, BLUEPRINT…), "EXPLODE"/peças do Aurex, "DIA n" no Vertex.
- `node scripts/i18n-visual.mjs <base>` (Playwright Chromium): 4 viewports (320/390/1440/2560) × 3 idiomas × 11 páginas — status, `lang`, overflow horizontal (0px em todas), erros de console, screenshots em `.audit/shots/`; seletor desktop em case e serviço (`/projetos/terral → /es/proyectos/terral → /en/work/terral → /projetos/terral`, `/servicos → /es/servicios`), cookie persistido, `/` honrando cookie, link direto respeitando URL, seletor alcançável por Tab, menu mobile com seletor visível e troca mantendo o case. **OK.**

## Limitações honestas

- Traduções EN/ES feitas por revisão editorial automatizada (subagentes) com regras de tom; vale uma leitura humana final em ES (variante neutra LatAm) antes de divulgar para esse mercado.
- `[lang]/not-found.tsx` (fallback de `notFound()` explícito dentro de páginas) ainda cai no shell genérico do Next no servidor — por isso rota desconhecida é decidida no middleware e vai para o 404 global. Nada no fluxo normal dispara `notFound()`.
- Firefox/Safari físicos continuam pendentes (ver 04-report).
