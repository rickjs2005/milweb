# MilWeb — site institucional (estúdio de desenvolvimento criativo)

Tipo: institucional
Stack: Next.js 15.1 · App Router · React 19 · Tailwind 3.4 · GSAP 3.15 (ScrollTrigger, CustomEase; SplitText sob demanda) · Lenis 1.3 · WebGL cru (shaders próprios, sem three.js) · WebAudio procedural · @vercel/analytics
Branch: `main` · produção em https://milweb.com.br (Vercel)
Último deploy verificado: **05/09/2026 · `5c3e71a`** (ato 06 validado em produção nesta data; ver bloco de 05/09). Anterior: **01/09/2026 · `a159c19`** — Google Preferred Sources no footer NO AR. **Em 02/09 (madrugada): ato 06 / Logistics Demo commitado e enviado a pedido do Rick (sem validação em produção — o notebook foi desligado logo depois; ver o bloco de 02/09).** Anterior: Google Preferred Sources no footer NO AR (validado em produção: CSP com `script-src`/`frame-src https://news.google.com`, um SDK em modo manual, Enter abre o popup do Google, SDK bloqueado cai no deep link; console só com o aviso de COOP do popup do Google). Anterior: `1ca1cb3` — ato 05/InkVision (validado em produção). Antes: `049ec4e` — LAB / Horizonte de Eventos (validado em produção). Antes: `8602dda` — ato 03/Vertex (validado em produção). Antes: `efc2654` — ato 02/Terral (validado em produção). Antes: `174883b` — ato 01/Kavita refinado (validado em produção em 1920 e 390). Antes: `d3cb274` — **Hero v4 no ar**: "CÓDIGOS MOVEM / O MUNDO.", o globo WebGL nasce do "O" da manchete e o Milo saiu do Hero (deploy `milweb-l1munnr8i`, aliasado em milweb.com.br; validado em produção nos dois breakpoints: `data-globe=on`, console limpo, sem overflow, composição idêntica ao local). Ver o bloco de 31/08 abaixo. Histórico anterior (Hero v3 com o Milo, `8c9e1df`) fica documentado nos blocos seguintes.

## 05/09/2026 — FECHAMENTO: ato 06 validado em produção, auditoria final, entrega documentada (commit + push; SEM deploy)

Sessão de fechamento do checklist. O que foi feito e verificado:

- **Ato 06 / Logistics Demo validado em produção** (`5c3e71a` = deploy `milweb-9w8wdq0dg`): `logistics.mjs https://milweb.com.br 1920x1080 390x844` → 16 checkpoints, zero colisão, zero corte, overflow 0, console limpo nos dois viewports; `logistics-hover.mjs` → retículo, resize 1920→1366→1920, reload e CTA abrindo `/projetos/logistics-demo` com `CADA MILHA SOB CONTROLE.`, console limpo.
- **Fase 05 fechada**: leitura completa do espanhol (`src/i18n/es.ts`, `services.ts`, `content.ts`, `content-projects-1/2/3.ts`, `diagnostico/page.tsx`). Variante neutra LatAm consistente ("cotización", "sucursales", "fincas", "posentrega"). Único ajuste: `FAIR_PRICE.sub` "cada real" → "cada centavo" (moeda brasileira não fala pro leitor hispânico). Não é revisão de falante nativo — registrado em `docs/entrega.md`.
- **Fase 11 — achado**: o painel Vercel → Analytics mostra o botão **Enable** e "Demo Data". O `<Analytics />` está no layout e o script `/_vercel/insights/script.js` responde 200, mas o recurso nunca foi ativado no projeto (API: `webAnalytics: {id}` sem `enabledAt`; MCP responde "Web Analytics not found"). **Todo `whatsapp_click` desde julho foi descartado.** Speed Insights está ativo e com dados. Adicionei `email_click` (mailto:) no mesmo listener de `track-conversions.tsx`. Ativar é 1 clique do Rick (decisão de conta, não fiz).
- **Fase 15 — achado**: Search Console tem a propriedade `sc-domain:milweb.com.br` verificada, mas a lista de sitemaps está **vazia** — o sitemap nunca foi enviado. `/sitemap.xml` em produção: 200, 114 `<loc>`, 456 `xhtml:link`, inclui `logistics-demo`; `robots.txt` aponta pro sitemap; 404 real responde 404. Enviar é um submit na conta do Rick (não fiz).
- **Lighthouse em produção (máquina com Edge aberto + WebView2)**: mobile 66 e 68 (duas execuções; LCP 3,0 s, TBT 0,9–1,1 s, SI 5,5 s) · desktop 86 / **96** / 96 / 100. O mobile local sem carga media 86 — a diferença precisa ser remedida em máquina limpa antes de virar diagnóstico. Sem terceiros bloqueando (third-party-summary vazio); o custo é do bundle próprio (chunk 3995 ≈ 0,8 s de script no mobile simulado).
- **Acessibilidade desktop caiu pra 96 — corrigido (pendente de deploy)**: (1) `[data-stage]` do hero usava `opacity: 0.16/0.28` → contraste 1.4:1; axe NÃO exime texto `aria-hidden` do contraste. Agora a hierarquia é por tom: futuro `ink-3`, passado `ink-2`, atual `ink` (todos ≥ 4.5:1 nos dois temas). (2) `label-content-name-mismatch`: `LanguageSwitch` tinha `aria-label="Español"` com texto visível "ES" → `aria-label="ES · Español"`; `SoundToggle` tinha `aria-label` longo com texto visível "SOM OFF" → `title`, nome acessível = texto. Lighthouse a11y local (dev 3100, desktop) = **100**; `tsc` e `next lint` verdes; hero conferido em screenshot 1440×900 (0 / 600 / 1400).
- **Fase 16 iniciada**: `docs/entrega.md` — URLs, repo, hosting, DNS (Registro.br → A 216.198.79.1), env vars, analytics/SC/Lighthouse com estado real, scripts de validação, backup, tag de versão, rotina mensal, pendências.

**Não feito de propósito** (decisões do Rick): ativar Web Analytics, enviar sitemap, `vercel --prod` (regra: deploy só com pedido), tag `v1.0.0` (só depois do deploy). Safari/Firefox físicos continuam sem aparelho.

## 02/09/2026 — LOGISTICS DEMO (ato 06 do Selected Work): cada milha sob controle (commit + push; produção NÃO validada)

Sexto card da seção `#work`: Kavita → Terral → Vertex → Aurex → InkVision → **Logistics Demo**. O projeto-fonte é `projetos/eduardo/logistic` (pasta ainda chamada assim; era o site do cliente KPM USA e foi COMPLETAMENTE desassociado em 01/09 — hoje é "Logistics Demo · A digital logistics experience · Concept by MilWeb", no ar em kpm-usa-site.vercel.app, domínio pendente do Rick). O brief do Rick pedia "06 / KPM USA" e coordenadas BR → MIA; como o projeto não tem mais nada disso (nome, rota e dados do cliente saíram, com teste que derruba a suíte), o card usa o que existe na fonte: nome **LOGISTICS DEMO**, nós **ORG-01 12°00′S 077°00′W → HUB-02 09°00′N 080°00′W → DST-03 41°00′N 074°00′W** (`content/logistics.ts`, `ROTA`), as sete etapas de `journey.ts`, os modais/capacidades de `CAPABILITIES`. Nada de cidade, cliente ou métrica.

**Assets reutilizados (todos da pasta `eduardo/logistic/public/media`):** recortes top-down `truck/ship/plane/container-top.webp` → `public/shots/logistics-demo/{truck,ship,plane,container}.webp` (reduzidos com sharp, alfa preservado, 13–30 KB; todos apontam para a DIREITA); `journey/s07-poster.webp` (porto ao anoitecer) → `port.webp` e `top-view/ship/desktop/editorial.webp` (navio cortando a esteira) → `ocean.webp`. Cover do case `public/shots/logistics-demo.webp` + galeria `journey/capabilities/system.webp` = capturas 1440×900 do site no ar (Playwright). Sharp: `projetos/atelier-vertex/node_modules/sharp` (o do `logistic` não resolve; milweb não tem).

**Integração (fonte única):** `content-projects-3.ts` (entrada `logistics-demo`, `landing-premium`, live, metric só com fatos da experiência, narrativa de 3 parágrafos + 4 highlights + galeria, tudo derivado do README/docs do projeto), `slugs.ts`, `projects.ts` (`ORDER` depois do inkvision, `FEATURED` += logistics-demo, `DISPLAY` = SCROLL EXPERIENCE — o globo é Three.js e a inferência daria WEBGL), `work.ts` (06, `CADA MILHA / SOB CONTROLE.` · `EVERY MILE / UNDER CONTROL.` · `CADA MILLA / BAJO CONTROL.`), i18n (`labels.logistics` = tupla de 2: modais + armazenagem/aduana; NOVO `work.stages.logistics` = tupla de 7 etapas "NOME · MODAL"), `page.tsx` (`WORLD_PLATE` porto, `WORLD_DETAIL` oceano, `WORLD_KEY` logistics; a metadata abre com `ORG-01 → DST-03` montado a partir de `NODES` da geometria — os códigos existem num lugar só; `stages` passa ao `WorkItem`), `act-config` (`ActSlug`, `SKIN` grafite `#0F1318` / off `#F4F3EF` / cinza `#7C7F85` = paleta `--ld-*` do demo, `ACT_ORDER`, `DotMode` **graticule** = cruzetas curtas regulares, `Dot.tickLen`), `act-stages` (switch → `logistics-stage.tsx`), `act-laws` (retículo + profundidade), `selected-work` (`logistics()`, `Residue` inkvision→logistics, ramo reduced-motion, `holdsToEnd` agora inclui o InkVision — mesmo motivo do Aurex: coreografado como último ato, `bottom bottom`, sem escurecida/sangria; o escuro já é o mesmo). **Hardcodes de quantidade:** nenhum `/ 05` (contador = `items.length`); seriais da PÁGINA deslocaram de novo: Logistics `MW/007`, LAB → `MW/008`, Built-with → `MW/010`, Contato → `MW/011`.

**Palco (`work/logistics-stage.tsx` + `work/logistics-geometry.ts`):** caixa 1200×380 (desktop; `w = min(88vw, 126.3svh)` para o SVG `meet` preencher exatamente a caixa e as anotações em % baterem) / 400×430 vertical no celular (`min(100vw − 2 margens, 42svh)`), duas composições renderizadas, só a visível é animada (`[data-route-h]`/`[data-route-v]`, escolhida por `small`). Rota = polilinha só com horizontais/verticais/45° (esquema de instrumento, não mapa): plano inteiro tracejado a 0,28 + três pernas `pathLength=1` que se desenham; nós com anel + ponto + pulso laranja `[data-pulse]` (uma vez); carga = um `<g data-cargo>` com cinco recortes `[data-unit]` (contêiner, caminhão, navio, avião, caminhão espelhado = `truckBack` do projeto) e o fixo laranja `#FF7A00` (único acento). Janela `[data-media]` = a superfície que a rota atravessa: porto → oceano (crossfade `[data-plate-b]`), com `view-transition-name` só na composição renderizada. Anotações em HTML (10/9 px mono) posicionadas em % da caixa com deslocamento em MARGEM e alinhamento num `<span>` interno — **armadilha:** a timeline anima `y` nelas e o GSAP escreve `transform`, então qualquer transform nosso no mesmo elemento (e a propriedade `translate` também não funcionou) era apagado; as legendas caíam em cima dos nós. Status `[data-step]` (as sete etapas, empilhadas no mesmo lugar; a anterior apaga 0,03 ANTES da próxima acender e os tweens nunca se sobrepõem — sobrepostos, o fade-in reaplicado por cima deixava STG-02 aceso para sempre) + `ROUTE 000 %` (proxy tween, texto por onUpdate; oculto no celular — colidia com o status a 375 px).

**Roteiro (último ato, `top bottom → bottom bottom`):** 0,02–0,18 fronteira (os tracking points do InkVision, resíduo `dotsOf("track")`, se alinham numa linha em y≈13,4 do viewBox 100×56 e apagam; plano sobe); 0,09–0,16 origem (ORG-01 + pulso, STG-01/02, contêiner → caminhão); 0,17–0,32 terrestre (perna, caminhão percorre vértice a vértice com rumo por segmento, tempo ∝ comprimento); 0,30–0,38 porto (janela abre em `inset(50% 0 50% 0) → 0`, handover: caminhão → contêiner); 0,36–0,54 oceano (perna longa, navio, janela vira mar aberto em 0,42); 0,54–0,58 hub (pulso, navio → avião); 0,57–0,70 aéreo (janela a 0,4; DST-03 aparece em 0,64 sem pulso); 0,705 pulso do destino, 0,72 STG-07, avião → caminhão espelhado, plano some, pernas a 1, janela a 0,28; metadata 0,66/0,72/0,78. Lei do cursor: retículo (h+v) só dentro da caixa, caixa k −6 / manchete k −3. Reduced: rota inteira, quatro nós e legendas, carga no destino com o rumo de chegada, janela no oceano, STG-07, 100 %.

**Validação (local, dev na 3100):** `scripts/.rsc-audit/work/logistics.mjs` (1920×1080, 1440×900, 1366×768, 1024×768, 390×844, 375×812 × checkpoints: overflow 0, console limpo, manchete sem corte, contador `06 / 06`, pernas/nós/recortes/status/percentual certos por checkpoint, ZERO colisão entre anotações e nada cortado; `--lang en/es`; `--reduced` em 1920 e 390) e `logistics-hover.mjs` (retículo acende dentro da caixa e apaga fora, hover do CTA, resize 1920→1366→1920 e reload preservam progresso 0,6 e a carga no mesmo ponto, CTA abre `/projetos/logistics-demo` com `CADA MILHA SOB CONTROLE.`). `/pt/work`, `/en/work`, case nas três línguas e `/projetos/logistics-demo` = 200; sitemap com o slug. `tsc` verde, `next lint` só com o aviso antigo do `<img>` do OG, `next build` passou. **NÃO validado em produção** (Rick pediu commit + push + desligar o notebook). **Próxima ação:** conferir o deploy da Vercel com `logistics.mjs https://milweb.com.br 1920x1080 390x844` e `logistics-hover.mjs https://milweb.com.br`; decidir o nome/rota se o Rick quiser outra coisa que "LOGISTICS DEMO" (o brief dizia KPM USA); quando o demo ganhar domínio próprio, trocar `live` em `content-projects-3.ts`.

## 01/09/2026 — GOOGLE PREFERRED SOURCES no footer (NO AR em `a159c19`)

Botão da MilWeb para o visitante adicionar milweb.com.br às fontes preferidas do Google, com a **implementação avançada oficial** (SDK em modo manual, UI nossa) e o **deep link oficial** como fallback.

**Arquitetura:** `src/lib/preferred-source.ts` (tipos do SDK — `init`/`addPreferredSource` —, `declare global` de `window.PREFERRED_SOURCE`, constantes do SDK/deep link/idiomas, e `loadPreferredSource()`: uma Promise por carga de página = um `push` na fila e um `init`, timeout de 6 s, `preferredSourceReady()` síncrono); `src/components/google-preferred-source.tsx` (client, `<a href=deep link target=_blank>` progressivo: com JS o clique prefere o SDK — síncrono se a API já chegou, para o popup nascer no gesto — e cai no deep link se falhar; estados idle/loading/error; SDK bloqueado no pré-aquecimento = clique vai direto ao link sem trocar o rótulo); `src/app/[lang]/layout.tsx` (`<Script src=publisher.js strategy="lazyOnload" preferred-sources-control="manual">`, uma vez por página, deduplicado na navegação client-side); `src/components/footer.tsx` (bloco `GOOGLE / FONTES PREFERIDAS` + `MANTENHA A MILWEB / NAS SUAS FONTES.` + `[ + ADICIONAR MILWEB ↗ ]` em `.act-cta`, acima das quatro colunas, régua suave embaixo); i18n `preferred.*` nas três línguas (`types.ts` tipado); CSP: `script-src` += `https://news.google.com` e `frame-src https://news.google.com` (o SDK injeta um iframe auxiliar dessa origem; sem a diretiva o console acusava "Framing violates default-src"). Idiomas do SDK pela lista oficial (`preferred-sources-languages.csv`): `pt-BR`, `en`, `es-419`.

**Sondado com o SDK real** (`scripts/.rsc-audit/work/ps-probe.mjs`): a API expõe só `init` e `addPreferredSource`; o clique abre um POPUP em accounts.google.com → news.google.com/swg/ui/v1/addpreferredsource (não há callback de conclusão — o evento `preferred_source_click` no Vercel Analytics registra clique e modo `sdk`/`deeplink`, nunca "adicionado"). Só placement `footer`: o footer está em todas as páginas (Home, /projetos, cases, serviços…), então não há duplicação.

**Elegibilidade — NÃO verificada:** milweb.com.br é domínio (formato elegível), mas o Google exige que o site apareça na ferramenta em google.com/preferences/source. Está documentado no código; ninguém assumiu que já aparece.

**Validação:** `scripts/.rsc-audit/work/preferred-source.mjs` (PT/EN/ES: eyebrow, rótulo e aria certos; 1920/1440/1366/390/375 sem overflow; um só `<script>` com `preferred-sources-control="manual"` mesmo após navegação client-side; foco por teclado + Enter abre o popup do Google; hover com régua e seta) e `--block` (SDK abortado: rótulo permanece, Enter abre `google.com/preferences/source?q=milweb.com.br`). Console: só o aviso "Cross-Origin-Opener-Policy would block window.closed" que vem da página do Google no popup. `tsc`/`lint` verdes, `next build` passou. **Próxima ação:** Rick aprovou e pediu commit + push (`a159c19`, validado em produção com `preferred-source.mjs https://milweb.com.br` normal e `--block`). Pendente do Rick: conferir se milweb.com.br aparece em google.com/preferences/source (elegibilidade real).

## 01/09/2026 — INKVISION (ato 05 do Selected Work): um sistema entendendo a pele (NO AR em `1ca1cb3`)

Quinto card da seção `#work`. A série passa a ser Kavita → Terral → Vertex → Aurex → **InkVision** (IA · computer vision · produto digital). Os quatro anteriores não mudaram de coreografia; conferidos em 0,55 depois da integração (contador `01 / 05`… automático).

**Integração:** `projects.ts` (`FEATURED` += inkvision), `work.ts` (entrada 05, título `ANTES / DA AGULHA.` · `BEFORE / THE NEEDLE.` · `ANTES / DE LA AGUJA.`), `i18n` (`labels.inkvision` = tupla de 3, só termos do case: `SEGMENTAÇÃO DE PELE · WARP · IMG2IMG · MULTI-TENANT · BULLMQ`), `page.tsx` (`WORLD_KEY`, `WORLD_PLATE`), `act-config` (`ActSlug`, `SKIN` bg `#121110`, `ACT_ORDER`, `DotMode` `track`), `act-laws` (comparação), `act-stages` (`InkvisionStage`), `selected-work` (`inkvision()`, `Residue` aurex→inkvision = pivôs, ramo reduced-motion, `holdsToEnd`), novo `work/inkvision-geometry.ts`. **Hardcodes de 4 encontrados:** nenhum `/ 04` (o contador é `items.length`); os seriais `MW/00n` são da PÁGINA, então deslocaram: InkVision `MW/006`, LAB → `MW/007`, Built-with → `MW/009`, Contato → `MW/010`. O case abre em `/projetos/inkvision` (fallback genérico de `case-stories`, `displayType` DIGITAL PRODUCT → "PRODUTO DIGITAL").

**Assets — decisão a registrar:** o projeto InkVision não tem NENHUMA mídia (`apps/web/public` só tem `icon.svg`; a captura é a landing com copy e um mockup pequeno). A placa `public/shots/inkvision/skin.webp` (1600×1067, P&B, 95 KB) foi **gerada no Higgsfield** (`soul_2`, 2 variantes, 1 crédito cada; escolhido o antebraço na diagonal). É material novo, não asset real — Rick decide se fica. O desenho da tatuagem é a marca do próprio InkVision (o losango com o ponto do `icon.svg`) em traço fino.

**Palco:** frame 3:2 (58 % de largura, teto 40 svh; celular 5:4 entre as margens) com a placa e, no MESMO viewBox (`xMidYMid slice` = `object-cover`): camada de visão (contorno de detecção, malha paramétrica do cilindro — 7 longitudinais + 9 anéis —, folha `DESIGN · 01`, 11 tracking points) e camada de tinta em `mix-blend-multiply` (preto sobre a folha branca é preto; sobre a pele, integra). O desenho é fatiado em 10 tiras; `stripTargets()` enrola cada uma no cilindro (posição `r·sin φ`, largura `cos φ`, curvatura `r(1−cos φ)·0,3`). Cópia "antes" com o véu dentro (`[data-sweep]`) que a varredura img2img clareia da esquerda para a direita, sem cobrir a tinta. Pipeline real à direita (`SKIN SEGMENTATION · PERSPECTIVE · CURVATURE · TATTOO WARP · IMG2IMG · LOW STRENGTH`), uma etapa por vez, com o coral do InkVision (`#F0563E`) só no ponto e na linha da varredura. Cursor: ORIGINAL ↔ SIMULATION (cópia clipada, lei escutando no painel).

**Roteiro** (último ato: `top bottom → bottom bottom`, entra 0→0,5, preso 0,5→1): 0,04 pele emerge (véu 0,55→0,28); 0,14–0,30 contorno + tracking points, etapa 01; 0,30–0,44 malha, etapa 02; 0,42 folha + desenho plano; 0,50 folha some, desenho viaja e gira para o eixo, etapa 03; 0,56–0,68 warp tira a tira; 0,64–0,76 varredura, véu 0, contorno some, sobram 3 marcações, etapa 04; 0,62–0,80 metadata; 0,80 comparação disponível. **Fronteira com o Aurex:** `holdsToEnd = last || aurex` mantém o `bottom bottom` do Aurex e o isenta da escurecida/sangria de saída (lavavam o calibre ainda em tela; o preto já é o mesmo); os dots-next do Aurex viram `track`, e o InkVision nasce com os pivôs como resíduo.

**Armadilhas desta rodada:** timeline do último ato sem tween até 1,0 (duração 0,86 → tudo adiantado; âncora `tl.set({}, {}, 1)`); `y: 0` num `<g transform>` apaga o translate; a coluna z-10 engole pointer da mídia; `act-stages.tsx`/`act-config.ts` são CRLF.

**Validação:** `scripts/.rsc-audit/work/inkvision.mjs` (1920×1080, 1440×900, 1366×768, 1024×768, 390×844, 375×812: overflow 0, console limpo, manchete sem corte, frame → manchete 130/52/44/40/283/266 px, tiras nas bordas a `scaleX 0,74`; `--reduced` = resultado estável), `inkvision-hover.mjs` (comparação clip 70 %/30 %, hover do CTA, resize 1920→1366→1920, reload na posição, CTA abre `/projetos/inkvision`), `act-shot.mjs` (Aurex 0,85/0,98 nítido; Kavita/Terral/Vertex 0,55 íntegros). `tsc`/`lint` verdes, `next build` passou. **Próxima ação:** Rick aprovou e pediu commit + push (`1ca1cb3`, validado em produção com `inkvision.mjs https://milweb.com.br`). A foto gerada ficou; trocar por fotografia própria é só substituir `skin.webp` e remedir `ARM` em `inkvision-geometry.ts`. (Em 02/09 o Selected Work ganhou o sexto ato — ver o bloco de 02/09; o InkVision passou a `holdsToEnd` como o Aurex, sem mudança de coreografia.)

## 01/09/2026 — LAB (ato 05) · Horizonte de Eventos: gravidade controlada (NO AR em `049ec4e`)

Só a section `#lab` foi tocada (`lab-teaser.tsx` reescrito, novo `features/lab/event-horizon.ts`, `.t-fit-lab` em `globals.css`, `page.tsx` só perdeu o prop `poster`). Header, Hero, cards e outras sections intactos.

**Causa do "bug do header":** não era vazamento de layer. O header é `fixed` com `mix-blend-difference`; a section rolava normalmente e as letras brancas gigantes invertiam o logo ("MILWEB® / MILWEB…") e a régua "GLSL / WEBGL" passava sob PT/EN/ES. **Correção:** a section virou trilho (220 svh; 180 no mobile) com palco `sticky` `isolate overflow-hidden` — durante o ato nada da manchete cruza o header, e no colapso final (0,88–1,0) a singularidade engole a manchete e apaga a régua antes de a section soltar; o que passa sob o header é só o fundo `#0B0B0B`. Medido: `underHeader = 0` em todos os checkpoints e `minOp = 0` após soltar.

**Protagonista:** o shader do buraco negro morava no canvas do compilador (`features/compiler`), que não monta mais desde o Hero v4 — a section era só preto (o poster ficava atrás do próprio fundo, `-z-20`). Agora `event-horizon.ts` (canvas 2D, sem segundo contexto WebGL): a malha de 64 px da MilWeb lenteada pela massa (deslocamento rs²·k/d, nunca cruza o anel — anel de Einstein), 26 partículas atraídas que renascem longe, halo off-white a 14 % até 2,1 raios, anel de fótons 1 px, aberração em dois cinzas deslocados 0,8 px, arco vivo em Signal Green a 32 %, núcleo `#0B0B0B` (só existe pela borda). Raio = 7,5 % do menor lado × (0,35 + 0,65·k), fecha no colapso. Só desenha com a section em tela (IntersectionObserver), DPR ≤ 1,5.

**Campo tipográfico:** uma função por distância (`letterTransform`): 0 além de `r0`, smoothstep até `r1`, e dentro o máximo — puxão até 14,5 % do corpo (22 px a 1920, 11 a 1024, 5 no celular), compressão 24 %, estiramento 10 %, inclinação até 7° rumo à massa. Raios e puxão escalam com o corpo da manchete (com 22 px fixos as letras se sobrepunham a 1024 e no mobile). Posições medidas UMA vez por layout (antes era `getBoundingClientRect` por letra por frame). Manchete em duas linhas desencontradas (linha 2 recuada 18 %, vão de 0,16 em) com `.t-fit-lab` (`--chars` = maior linha / 0,82): a massa mora no vão, no centro do h2.

**Roteiro (progresso do trilho, `top top → bottom bottom`, com damping 0,14):** 0–0,20 estável, campo 0; 0,20–0,45 campo 0→0,55; 0,45–0,62 →1; 0,62–0,72 pico; 0,72–0,86 →0,4; 0,86–1,0 →0,6 e colapso 0,88–1,0 (letras vão ao centro, escala 0,15, opacidade 0; régua apaga; raio fecha). Cursor (ponteiro fino): a massa anda 8 % e o CTA é puxado até 3 px a 180 px do ponteiro (`quickTo` x/y). CTA virou `.act-cta` (`[ ENTRAR NO LAB ↗ ]`, mesma microinteração dos quatro atos). Rodapé em flex (a grade de 12 quebrava o CTA em 1024). Reduced-motion / `quality=low`: um frame parado (k 0,7), sem SplitText.

**Armadilhas:** `[data-stage]` é selecionado pelo `build-hero.tsx` (deixou o palco a 0,16 de opacidade) e `[data-line]` está no `SELECTOR` de `webgl/interface-texture.ts` — no LAB são `data-lab-stage`/`data-lab-line`.

**Validação:** `scripts/.rsc-audit/work/lab.mjs` (1920×1080, 1440×900, 1366×768, 1024×768, 390×844 × 3–7 checkpoints: overflow 0, console limpo, letras sob o header 0, puxão máximo 22/22/22/11,5/4,9 px, rotação ≤ 6,8°; `--reduced` = anel parado entre as linhas) e `lab-hover.mjs` (cursor esquerda/direita muda a letra mais puxada, CTA atraído, resize 1920→1366→1920 e reload na posição). `tsc`/`lint` verdes, `next build` passou (`/[lang]` 193 kB, compartilhado 106 kB).

**Limitações:** (a) a régua "LAB / 001 · GLSL / WEBGL" e o "MW/006" ficaram como estavam (consistência global); (b) o colapso final engole a manchete em vez de devolvê-la ao normal — foi a forma de proteger o header sem tocar no blend global; (c) aparelho físico não testado. **Próxima ação:** Rick aprovou e pediu commit + push (`049ec4e`, validado em produção com `lab.mjs https://milweb.com.br`). Pendentes da Home: Aurex (ato 04) na anatomia dos outros três; o resto das sections (Capabilities, Break, Human, Built With, Contato) só quando o Rick pedir.

## 01/09/2026 — VERTEX (ato 03) refinado: o projeto construído pelo scroll (NO AR em `8602dda`)

Só o ato 03 foi tocado. Compartilhado mudou em três pontos cirúrgicos: `ActStage` recebe `labels`; o `<ul>` de metadata não renderiza para o Vertex (as anotações vivem no palco); `Residue` vertex→aurex (no painel do Aurex) passou a desenhar o feixe convergido em que o Vertex termina; ramo reduced-motion com `if (world === "atelier-vertex")`. O tipo `labels.vertex` virou tupla de 5.

**Causa de raiz:** a "foto" era a captura do site da Vertex (`entregue.webp`, com copy e overlay escuro), fatiada em janelas que mostravam carro, céu e trechos incoerentes. O projeto-fonte (`projetos/atelier-vertex/public/hero/obra.mp4`, 1024×536, 5 s) é a obra em câmera FIXA indo do andaime ao prédio pronto. Frames extraídos com ffmpeg (t = 0 e 3,8 s), recorte 1024×380, monocromia com contraste aberto (`grayscale + linear(1.12,−14)`) → `public/shots/atelier-vertex/{estrutura,entrega}.webp` (54/41 KB). `WORLD_PLATE`/`WORLD_DETAIL` apontam para eles; `entregue.webp`/`prancha.webp` seguem na página do case.

**Palco (`act-stages.tsx` → `VertexStage`):** a prancha inteira é um invólucro `[data-media-wrap]` (28 % → 94 %, `aspect 1024/380`, `max-h 40svh`, `top max(15svh,136px)`): 5 guias verticais = bordas das 4 fatias (100 svh, desenham de cima para baixo), 2 horizontais = bordas do frame (crescem do centro), 10 anchor points nos cruzamentos com `[data-anchor-fill]` (□ → ■ → □ quando a guia chega), cota horizontal `11,50 M` sob o frame, cota vertical `3,20 M` à direita (some abaixo de 1280 px — não cabe na margem), elevação da fachada em SVG traçada sobre o recorte (`xMidYMid slice`, casa com o `object-cover` em qualquer proporção). Cada fatia tem duas camadas com a imagem inteira deslocada (`width 400%`, `left −k·100%`): `[data-layer-a]` estrutura, `[data-layer-b]` entrega. Fora do frame: guia de margem em 6 %, `ESC 1:75` / `REV 03` acima do frame (`max(11svh,104px)` — 11 % puro colidia com o rail a 768 px), `PAV. TIPO` com linha de chamada tracejada, `GOP 1`. Mobile: frame entre as margens em 4/3 (cover recorta as laterais; a elevação recorta igual), ESC/REV e a cota horizontal ficam; anchors, chamada e cota vertical somem.

**Roteiro (`selected-work.tsx` → `vertex()`):** 0,02–0,26 prancha (margem, verticais com stagger 0,03, horizontais, anchors no instante em que a guia cruza — 15 % e 55 % do percurso —, rótulos, chamada); 0,14–0,36 elevação se traça; 0,30/0,35/0,40/0,45 estrutura sobe do chão (`clip-path inset(100% 0 0 0) → 0`, cada fatia assentando 5 %); 0,44/0,50 cotas; 0,46/0,51/0,56/0,61 entrega cobre a estrutura pelo mesmo caminho, elevação cai a 0,25; 0,66–0,74 assinatura (elevação, rótulos, cotas e anchors saem); 0,72–0,96 saída: verticais convergem (`x` por função da largura do frame, sobrevive ao refresh) e giram ±9° por guia, horizontais encolhem a 0,12, obra apaga a 0,3, frame recua a 0,94 — sobre a sangria compartilhada papel → preto. Lei do cursor: perspectiva no invólucro (rotationX/Y) e a manchete contra.

**Metadata:** `ESC 1:75 · REV 03 · 11,50 M · GOP 1 · PAV. TIPO` viraram cinco anotações independentes nas três línguas (ES `PLANTA TIPO`; o "CADA FRAME ES KEYFRAME" que estava lá saiu). **`MW/004` mantido**: serial da página, consistente com o código.

**Validação:** `scripts/.rsc-audit/work/vertex.mjs` (1920×1080, 1440×900, 1366×768, 1024×768, 390×844 × 9 checkpoints: overflow 0, manchete 151/117/108/84/37 px sem corte, console limpo, frame → manchete 130/118/44/146/277 px; `--reduced` = prancha inteira, obra entregue nas quatro fatias, cotas e elevação leve) e `vertex-hover.mjs` (cursor inclina o invólucro, hover do CTA, resize 1920→1366→1920 e reload preservam progresso 0,6 e clips). `tsc`/`lint` verdes, `next build` passou (`/[lang]` 191 kB, compartilhado 106 kB). Saída conferida em 0,9 e 0,96 (feixe + Aurex entrando).

**Limitações:** (a) o vídeo tem 1024 px de largura — a 1920 o frame amplia 1,14× (teto de nitidez do asset, igual ao que estava no ar); (b) a elevação é traçado editorial, não projeto; (c) aparelho físico não testado; (d) o feixe da saída fica a 0,22 no resíduo do Aurex — discreto por desenho. **Próxima ação:** Rick aprovou e pediu commit + push (`8602dda`, validado em produção em 1920 e 390 com `vertex.mjs https://milweb.com.br`). Falta o Aurex (ato 04) — mesma anatomia: asset real do projeto antes de captura, invólucro dono do movimento, tudo antes de 0,72.

## 01/09/2026 — TERRAL (ato 02) refinado: duas figuras com destino (NO AR em `efc2654`)

Só o ato 02 foi tocado (Hero, header, i18n-sistema, Kavita/Vertex/Aurex intactos; o compartilhado mudou só em `dotsOf("grain")` — que é a camada `data-dots-next` do Kavita — e no ramo reduced-motion, com `if (world === "terral")`). O problema de raiz era o mesmo do Kavita: as duas mídias eram recortes borrados da CAPTURA do site (1440 px ampliados 1,5–2×), e a composição não dizia o que cada uma era.

**Assets (assados com sharp, script no scratchpad):** `public/shots/terral/grao.webp` (1500×800, 41 KB — grãos caindo no resfriador com o fogo embaixo, de `projetos/terral/public/shot/tambor/a.webp`) e `torra.webp` (1250×640, 54 KB — as mãos no braço do resfriador, de `tambor/b.webp`). `page.tsx` ganhou `WORLD_PLATE.terral` e `WORLD_DETAIL.terral` aponta pra `torra.webp`; `sol.webp`/`casa-do-torrador.webp` seguem na página do case. O projeto Terral ainda tem `terreiro/`, `moenda/`, `xicara/`, `caparao/`, `full.mp4` por cena e recortes COM alfa em `cutout/` — não usados.

**Palco (`act-stages.tsx` → `TerralStage`):** FIG. 01 (grão) em `left 50% / right 6% / top 18.67% / h 40%`, FIG. 02 (torra) em `left 28% / top 34% / w 27% / h 22%` — as bordas caem nas guias 720/1354/168 e 403 do viewBox do Vertex. Cada figura é um INVÓLUCRO (`[data-media-wrap]`/`[data-media-b-wrap]`, dono do movimento: timeline em yPercent/xPercent, cursor em x/y) com a CAIXA clipada dentro (`[data-media]`, clip-path + view-transition) e `[data-crop]` (zoom + deriva interna). Filhos do invólucro: `[data-frame]` (borda que endurece na saída), 8 `[data-mark]` (marcas de corte), `[data-line-v]`/`[data-line-h]` (as bordas estendidas até a tela, `scale 0→1` da própria borda), rótulo `FIG. 0n`. `[data-fibre]` (a malha fina) ganhou máscara radial: inteira ao redor das figuras, ~30 % atrás da manchete. `[data-paper]` = ruído de impressão a 0,08 na folha inteira. `[data-roast]` = curva de torra só com eixos °C/MIN e a marca `1ST CRACK` (sem números — nada que leia como dado do cliente), alinhada à guia 86. Mobile: as duas figuras (FIG. 01 `left 18% / top 14% / h 36%`, FIG. 02 `left margin / top 44% / w 62% / h 22%`), sem marcas/linhas/instrumento.

**Roteiro (`selected-work.tsx` → `terral()`):** 0,02–0,22 papel (fibra 0→1, ruído 0→0,08); 0,08–0,30 FIG. 01 entra pela direita (`inset(0 0 0 100%)` → 0) com a foto deslizando dentro no sentido contrário; 0,26–0,42 FIG. 02 cruza de baixo/esquerda e assenta; 0,30 grão sobe (0,5); 0,36–0,86 profundidade — FIG. 01 yPercent −3→0 + zoom 1→1,06 + xPercent 0→−1,5, FIG. 02 yPercent +2→−9 + xPercent 0→2, malha de grãos `y` 0,6→−1 (unidades do viewBox); 0,40 marcas, 0,30/0,44 rótulos FIG., 0,44–0,62 curva se desenha, metadata em 0,46/0,52/0,58; 0,62 instrumento sai (manchete domina por subtração); **0,66–0,86 saída**: grão/ruído/fibra limpam, bordas endurecem (0,7), marcas e rótulos somem em 0,69, verticais se estendem 0,68–0,80 e a horizontal 0,72–0,82, fotos baixam a 0,38 em 0,72–0,86. A saída começa em 0,66 e não em `hold+0.1` porque a metade de cima sai da tela em ~0,72 e a sangria de cor do Vertex passa de 50 % em ~0,9 — na primeira rodada as linhas nasciam em 0,80 e ninguém as via. Lei do cursor (`act-laws.ts`) move os invólucros (k −7 / 13).

**Metadata:** `CASA DO TORRADOR · 05 CAPÍTULOS · 03 BLENDS` nas três línguas — "SEGURE 6S / HOLD 6S / MANTÉN 6S" saiu (a interação de segurar existe só na página do case; no card era instrução falsa). **`MW/003` mantido**: é o serial da PÁGINA (Hero 001, Kavita 002, Terral 003…), consistente com o código; índice por card (`MW/002` no Terral) mexeria nos quatro atos — decisão do Rick.

**Validação:** `scripts/.rsc-audit/work/terral.mjs` (1920×1080, 1366×768, 1440×900, 390×844 × 6–8 checkpoints: overflow 0, manchete 151/108/126/38 px sem corte, console limpo, FIG. 02 → metadata 21–108 px de respiro; `--reduced` = estado estável com figuras, marcas e curva prontas) e `terral-hover.mjs` (cursor move os invólucros em sentidos opostos, hover do CTA, resize 1920→1366→1920 e reload na posição preservam progresso 0,55). Único aviso: o "LCP image without priority" do Next em dev quando o reload cai já dentro da seção — heurística de dev, não existe em produção. `tsc`/`lint` verdes (só o warning pré-existente do OG image), `next build` passou (JS compartilhado 106 kB). Saída do Kavita conferida em 0,86 com a malha de grãos nova.

**Limitações:** (a) aparelho físico não testado; (b) vídeo `tambor/full.mp4` (425 KB) existe e daria "matéria viva" de verdade na FIG. 01 — não usado (imagem com zoom/deriva lenta); (c) o gap da metadata (`gap-x-5`) é compartilhado e ficou como está; (d) `sol.webp` continua sendo o hero do case, então a view transition morfa de `grao.webp` para ele (mesmo comportamento do Kavita). **Próxima ação:** Rick aprovou e pediu commit + push (`efc2654`, validado em produção em 1920 e 390 com `terral.mjs https://milweb.com.br`). Seguir para o Vertex (ato 03) na mesma anatomia.

## 01/09/2026 — KAVITA (ato 01) refinado: janela de varredura + drone com alfa (NO AR em `174883b`)

Só o ato 01 foi tocado (Hero, header, i18n e os outros três atos intactos; o que é compartilhado mudou só onde o Kavita precisava e sem efeito nos demais). O problema de raiz era "janela com imagem + drone por cima": a placa era um recorte de 540 px da CAPTURA do site (borrado a 1,56×, com o header e o botão teal aparecendo no parallax) e o drone era um recorte de 305 px do card de produto com `multiply` — o tanque branco sumia sobre o verde.

**Assets (assados com sharp, scripts no scratchpad — não versionados):** `public/shots/kavita-drones/field.webp` (1160×490, 104 KB) = poster do hero do site institucional da Kavita (`projetos/kavita-institucional/public/images/hero-video-poster.webp`, cafezal em MG com horizonte/morros/fileiras, SEM drone), saturação 0,84 e contraste um ponto abaixo; `public/shots/kavita-drones/t70p.webp` (900×442, 47 KB) = render DJI Agras T70P (`projetos/kavita-drones-landing/assets/drones/t70p.webp`, 1200 px) com **canal alfa** gerado por chave de quase-branco + remoção de franja. Tentativa descartada: recortar o drone do frame do vídeo `drone-gemini.mp4` por croma — o solo sombreado entre as fileiras tem a mesma luminância/croma do trem de pouso, máscara suja. `hero.webp`/`drones.webp`/`equipamentos.webp` seguem em uso na página do case (`case-stories.ts`).

**Palco (`act-stages.tsx` → `KavitaStage`):** janela `[data-media]` 60 % × 44 % sangrando à direita, topo `max(15%, 136px)` (a 768 px de altura 15 % encostava no contador `01 / 04`), imagem 10 % mais alta que a caixa (parallax sem borda), régua de marcas no topo + rótulo `SCAN 01`; `[data-scan]` = invólucro que desliza (yPercent 0→100) carregando a linha (1 px signal + banda) E o véu do "não lido" (papel a 74 %) — um dono só; drone `[data-media-b]` 28 % com `aspect-[900/442]` e o alvo `[data-target]` como FILHO (herda o movimento sem ter transform próprio); 4 leituras em `[data-readout]` (LAT/LON · T70P ALT 42 M · SWATH/COV · SPRAY SYSTEM ACTIVE) à direita, só desktop. Metadata: `li[data-meta]` (era `data-coord`, que misturava rótulos e leituras num só stagger que terminava em 0,76 — o "T25P" aparecia apagado em 0,5).

**Roteiro (`selected-work.tsx` → `kavita()`):** 0,04–0,26 contornos/rotas se desenham (rotas viram pontilhado via `set` de dasharray, reversível); 0,06–0,24 janela abre como FENDA (`inset(50% 0 50% 0)` → 0); 0,22–0,56 varredura; 0,32–0,46 drone entra de baixo (`yPercent/xPercent/scale/autoAlpha` — x/y são da lei do cursor, que ganhou o drone como 4ª profundidade, k −18); leituras em 0,28/0,46/0,53/0,60, a anterior cai a 0,16; metadata 0,48/0,54/0,60; 0,36–0,86 topo respira (yPercent −4, scale 1,035, anéis em opacidade do centro para fora); **0,64–0,74 a janela fecha como abriu e o drone decola** — porque o conteúdo é sticky num trilho de 200 svh e a metade de cima sai da tela em ~0,72: a dominância da manchete é por subtração; 0,72–0,92 **contornos viram grão** (`stroke-dasharray "1 1" → "0.004 0.014"`, width 0,75→1,6, `pathLength=1`) — a língua do Terral nascendo no Kavita; o `Residue` kavita→terral agora desenha esse mesmo estado pontilhado.

**Compartilhado, mínimo:** `.act-cta` — régua só sob a palavra (`.act-cta-txt::after`), colchetes ±4 px, seta 3 px com 40 ms de atraso; `stillness` (reduced-motion) ganhou `[data-meta]`/`[data-frame]` e um `set()` que pula alvos vazios (6 avisos "GSAP target not found" no console eram pré-existentes: `[data-grain]`/`[data-slice]` só existem em um ato cada).

**Validação:** `scripts/.rsc-audit/work/kavita.mjs` (8 checkpoints × 1920/1366/390: overflow 0, manchete 132/93/31 px sem corte, console limpo, `--reduced` = estado estável com tudo legível) e `kavita-hover.mjs` (hover do CTA, resize 1920→1366→1920 no meio da cena e reload na posição: progresso e estado preservados). `tsc`/`lint` verdes (só o warning pré-existente do OG image). `next build` passou na Vercel. **Próxima ação neste projeto:** Rick aprovou o Kavita como base de qualidade; os atos 02–04 (Terral, Vertex, Aurex) seguem a mesma anatomia quando voltarmos — o Rick pausou pra outro projeto.

**Decisões para o Rick:** (a) `MW/002` mantido — é o serial da PÁGINA (Hero 001 → atos 002–005 → Lab 006 → Built-with 008 → Contato 009); virar `MW/001` colidiria com o Hero, e mudar o formato mexe nos quatro atos; (b) as coordenadas `LAT 19°55′ S · LON 43°56′ W` são decorativas (região de BH/MG); (c) a placa vem do site institucional (mesmo cliente, Kavita Agro), não da landing dos drones; (d) 1440×900/1600×900 e aparelho físico não varridos.

## 31/08/2026 — SELECTED WORK v2: quatro atos, um filme (local, sem push)

Refatoração completa da seção `#work`. Antes: quatro `<article>` de 100 svh, todos `sticky top-0`, com duas timelines por painel. **Causa raiz do "card → card → card": o painel *i* grudava no topo no MESMO frame em que o *i+1* começava a subir por cima — nenhum ato ficava sozinho em tela em momento nenhum.** Não era falta de motion, era falta de duração.

**Ritmo novo.** Cada ato tem trilho de `200 svh` com o conteúdo `sticky` dentro: uma viewport PARADO + uma viewport sendo coberto. Uma ScrollTrigger por ato (`top bottom` → `bottom top`, 3 svh de percurso), progresso 0→1, tudo derivado das faixas de `work/act-config.ts` (0.18 entrada · 0.36 construção · 0.70 experiência · 0.86 preparação · 1.00 transição). O ÚLTIMO ato usa `end: "bottom bottom"` — ninguém o cobre, e terminar em `bottom top` deixava um terço do progresso acontecendo já fora da tela.

**Anatomia comum** (a mesma nos quatro; o que muda é o palco): rail `01 / KAVITA … 01 / 04` (nível 03) · palco full-bleed · metadata técnica (nível 02) · manchete que domina (nível 01, `.t-fit-work` escala por `--chars` com teto em `14svh` → 132–151 px em 1920, contra 120 px fixos antes) · `MW/00n` + tipo + `[ EXPLORAR PROJETO ↗ ]`.

**Palcos.** Kavita: faixa da lavoura sangrando à direita + o drone isolado (do frame de produtos, `mix-blend-multiply` no invólucro para dissolver o card branco no papel) com marcação de alvo, topografia, varredura e leituras. Terral: duas fotografias SOBREPOSTAS em velocidades diferentes (macro dos grãos escala 1→1.05, tambor do torrador translada), papel só no fundo, grão dentro das caixas de imagem. Vertex: 4 guias que DEFINEM 4 fatias revelando a fachada entregue sobre a prancha, com anchor points, rótulos e linha de cota. Aurex: composição escura preservada; o vazio do centro passa a ser o calibre AX-01 em vista explodida (SVG procedural, 9 peças, engrenagens com razão de trem real — `work/aurex-movement.tsx`), alinhado com a fotografia no estado montado.

**Transições.** A cor do próximo mundo sobe durante TODA a cortina (`[data-bleed]`, 0.70→0.96, para em 0.9 de opacidade), o painel que entra nasce com um véu da cor do anterior no topo (`[data-veil]`), o motivo do ato anterior sobra nos primeiros frames (`Residue`), e a malha de pontos troca de significado antes da virada (survey → grain → anchor → pivot, `work/work-dots.tsx`).

**Quatro defeitos de raiz encontrados e corrigidos:**
1. **`refreshPriority` no pin do Hero.** As triggers dos atos são criadas num `requestIdleCallback` ANTES de o pin do Hero existir, e o ScrollTrigger nunca somava a distância do pin a elas: os quatro atos nasciam com `start` 3456 px adiantado e chegavam ao estado final antes de o visitante ver o primeiro. `ScrollTrigger.refresh()` não corrige — a ordenação do refresh usa exatamente esse campo. Uma linha em `build-hero.tsx` (é a única alteração no Hero).
2. **`vector-effect` não é herdado em SVG.** O `stroke` de 1 unidade do viewBox no `<g>` da malha virava ~19 px de traço em volta de cada ponto: os "pontos" eram borrões de 24 px em todos os atos.
3. **Recorte × GSAP disputando `transform`.** O recorte por `scale`/`transform-origin` era sobrescrito pelo `scale` da timeline e a Terral voltava a mostrar o frame inteiro do site. O recorte virou geometria (`left/top/width/height` a partir da região em PIXELS da captura) e o `transform` ficou livre para o movimento.
4. **`sizes` do next/image com zoom.** Declarando a largura da CAIXA (30vw) o Next servia variante de 640 px para uma imagem exibida com 2000 px — todo recorte fechado saía borrado.

**Art direction dos assets:** as capturas dos projetos são páginas inteiras, e era isso que fazia tudo ler como "screenshot num retângulo". Cada palco agora recorta uma região em pixels escolhida para EXCLUIR a interface do próprio site (a copy da Kavita cobre metade do frame; a tipografia da Terral fica na metade esquerda; o botão e a manchete do Vertex ficam acima da faixa usada). `page.tsx` ganhou `WORLD_DETAIL` por ato — o Vertex estava usando `entregue.webp` como base E como fatia (a prancha nunca aparecia).

**Arquivos:** novos `src/sections/home/work/{act-config.ts, act-stages.tsx, aurex-movement.tsx, work-dots.tsx, act-laws.ts}`; `selected-work.tsx` reescrito; `world-layers.tsx` e `world-laws.ts` removidos; `globals.css` (tokens `.act`, `.t-fit-work`, `.act-cta`), `page.tsx`, i18n (`enter` → EXPLORAR PROJETO / EXPLORE PROJECT / EXPLORAR PROYECTO; vertex ganha PAV. TIPO), `build-hero.tsx` (só o `refreshPriority`).

**Validação:** `scripts/.rsc-audit/work/{acts.mjs, reduced.mjs, probe.mjs}` — 1920×1080, 1366×768 e 390×844 em 4 checkpoints por ato: sem overflow, console limpo, manchete sem corte, CTA acessível; reduced-motion com estado estável (calibre aberto, fatias reveladas, véu/sangria apagados). `tsc`/`lint`/`build` verdes; JS compartilhado segue 106 kB, rota `/[lang]` 188 kB. **Sem push.**

**Limitações:** (a) a seção passou de 400 svh para 800 svh — é o preço do ato ter tempo de existir, e é uma mudança de ritmo que o Rick precisa aprovar; (b) 1600×900, 1440×900 e 1024×768 não foram varridos (a varredura larga estava custando minutos por rodada); (c) as capturas de origem têm 1440×900, então recortes fechados chegam a ~1,5x de ampliação — aceitável, mas é o teto de nitidez disponível; (d) navegação para os cases, voltar pelo browser e troca de idioma dentro da seção não foram re-testados nesta passagem.

## 31/08/2026 — HERO v4: "CÓDIGOS MOVEM O MUNDO." (local, sem push)

Nova direção conceitual pedida pelo Rick: **o Milo saiu do Hero** e a manchete virou `CÓDIGOS MOVEM / O MUNDO.` (EN `CODE MOVES / THE WORLD.`, ES `CÓDIGOS MUEVEN / EL MUNDO.`), sub `Design, código e tecnologia para construir o que vem depois.` A ideia é que **o mundo nasce da própria tipografia**: o "O" que fecha a palavra do mundo não é trocado por um globo — ele *vira* um.

**Como o "O" vira globo.** O glifo alvo é dado de idioma (`hero.orb: [linha, caractere]` no dicionário: PT `[1,6]`, EN `[1,5]`, ES `[1,7]` — sempre o "O" perto do fim da linha, para a migração ser curta). Ele é renderizado num `<span data-orb>` com uma **régua de largura zero** dentro (`<i data-orb-cap>`, `height: 0.72em`, `vertical-align: baseline`): o `bottom` dela é a linha de base EXATA e o `top` é o topo da caixa alta, independente das métricas da fonte que o navegador acabou usando (`getBoundingClientRect` de um inline devolve a caixa de conteúdo da fonte, não a do glifo — sem a régua o anel nasce alguns px fora e a troca "pisca"). As proporções do glifo saíram de `measureText`/`actualBoundingBox*` na fonte e nos eixos reais (`widthRatio 0.785`, `overshoot 1.005`, `centerRatio 0.475`, `stroke 0.56` — a Archivo Black tem haste de 0,60 do raio), medidas em 1920/1366/390 por `scripts/.rsc-audit/globe/glyph-metrics.mjs`. O canvas desenha um anel com essa geometria; a troca glifo↔anel dura 0,02 de progresso e não tem salto para ver. Depois a haste **afina** até virar a silhueta de 1 px enquanto o vazio interno vira volume — não há troca de figura em nenhum ponto.

**O globo.** WebGL 1 cru no MESMO renderer do site (`src/webgl/renderer.ts`), sem three.js: um quad de tela inteira com esfera analítica (`src/webgl/globe-frag.ts`), descarte barato em `r² > 1.35` (95 % dos pixels saem em ~10 instruções). Continentes em matriz de pontos distribuída por área (colunas por linha caem com `cos(lat)`), lidos de `src/webgl/earth-mask.ts` — contornos em (lon, lat) rasterizados num canvas 256×128 em runtime (≈4 KB de código contra ~40 KB de PNG, sem request). Tinta sobre papel: sem azul, sem bloom, sem specular; o único acento é o marcador do Brasil em Signal Green. Orientação **determinística e ancorada**: `spinHome = −2,407 rad` deixa o Atlântico Sul de frente quando o globo se forma (com `uTilt = −0,36`, a longitude no centro é `spin + π/2`), a migração adiciona 0,85 rad (resposta ao scroll) e a "vida" é uma deriva **limitada** de ±0,22 rad — rotação livre daria a volta em 2 min e esconderia o marcador a maior parte do tempo, além de ler como demo de Three.js.

**Timeline** (`HERO_SCENE`, desktop): 0.18 design · 0.40 focus · 0.58 morph · 0.72 sphere · 0.86 formed · 0.94 outro. Mobile (`HERO_SCENE_MOBILE`, pin 220 %): 0.14/0.36/0.52/0.66/0.82/0.90. O globo migra num **arco com o eixo X na frente** (`mx = 1−(1−m)^2.4`, `my = 1−(1−m)^1.5`, raio por smoothstep): ele nasce dentro da palavra, então qualquer caminho cruza a manchete — o que decide se lê bem é o TAMANHO na travessia. Em 0.78–0.84 **a letra se refaz**: o "O" volta ao seu lugar (a manchete precisa ler "O MUNDO." em repouso, não "O MUND ."). Saída: o globo NÃO se apaga, cresce e sangra pela lateral com `fade 0.62` — apagá-lo deixava a metade direita vazia exatamente no frame da ENTREGA.

**Armadilhas encontradas e corrigidas (as caras):**
- **`globe.sync()` ligado ao scroll não funciona com `scrub`.** O ScrollTrigger avisa quando a POSIÇÃO muda; os valores só chegam ~0,8 s depois. Um salto seguido de parada chamava `sync()` com `fade` ainda 0, o rAF nunca ligava e o globo simplesmente não era desenhado. Agora o `sync` é o `onUpdate` da **timeline**.
- **A máscara do SplitText cortava acentos.** `mask: "chars"` cria caixas com `overflow: clip` na altura da linha (0,86 em) — o acento de "CÓDIGOS" mora acima da caixa alta e a manchete lia "CODIGOS" (defeito que já existia com "CÓDIGO DEVE"). A máscara agora é liberada quando a revelação termina.
- **`col-span-*` do Tailwind zera `col-start`.** O utilitário emite o atalho `grid-column`, que anula um `md:col-start-1` de breakpoint anterior — era isso que jogava o CTA para a coluna 4, ao lado da sub. A ENTREGA virou um bloco simples de largura fixa (≈320 px), CTA abaixo.
- **A letra só pode sumir se houver anel.** O `globeFrame.glyph` só é aplicado ao DOM quando `globe.mounted` — sem GPU, apagar o "O" deixaria "MUND." para sempre.
- **`build` e `dev` na mesma `.next`** quebram o dev server (já registrado); foi preciso matar o processo e limpar `.next`.

**Composição.** Desktop: manchete alinhada à esquerda ocupando ~61 % da largura (continua o maior elemento), globo em `cx 0.77 / cy 0.37` com raio `min(0.145 W, 0.25 H)` — os dois tetos garantem que a borda esquerda dele caia em 0,63 W em 16:9, 16:10 e 4:3, sem nunca alcançar o texto. O wireframe da direita (a caixa da figura) saiu; no lugar entrou a **órbita**: círculo tracejado, anel-alvo no raio exato do globo, elipse orbital, marcas de eixo e `MW/GLOBE · LAT −15.79 · LON −47.88`. Código decorativo agora tem `<script> const world = build(); world.move(); </script>`. Mobile: `CÓDIGOS / MOVEM / O MUNDO.` (só as linhas anteriores à do globo quebram por palavra), manchete até a margem, e o globo pousa numa **faixa livre medida no DOM** — entre a base da ENTREGA e o topo do rodapé, ou, quando essa faixa não existe (320×568, 375×667: ENTREGA e rodapé se encostam), na faixa ACIMA da manchete. Sempre 35–45 vw, nunca sobre texto.

**Preloader**: `exit` de 5 s → **3 s** (FIRST e QUICK; SHORT/mobile continua 2 s).

**Validação** (`scripts/.rsc-audit/globe/`, não versionado): `layout-check.mjs` mede sobreposição real (Range sobre as linhas, não a caixa do h1), overflow e posição do CTA em 11 viewports × 6 checkpoints — todos limpos; `hero-frames.mjs` captura 0/20/40/60/75/90/100 %; `states.mjs` cobre reduced-motion, `?quality=low` (SVG estático assume, manchete inteira) e as 3 línguas; `reverse.mjs` prova reversibilidade, salto direto e resize no meio da cena; `handoff.mjs` mostra a passagem para o Selected Work e mede 30 fps no canvas com **0 frames fora da tela** (IntersectionObserver corta o rAF); `mask-preview.mjs`/`mask-zoom.mjs` conferem os continentes. `tsc`/`lint`/`build` verdes; JS inicial compartilhado segue em 106 kB, rota `/[lang]` em 186 kB. **No ar em `d3cb274`** (junto foi o fix do exit dos cases do Selected Work, `c992f1b`, que estava local desde a investigação do travamento).

**Pendência operacional:** Production ainda tem `NEXT_PUBLIC_HERO_VISUAL=milo`. Não quebra (valor desconhecido cai no padrão `globe`), mas o certo é `vercel env rm NEXT_PUBLIC_HERO_VISUAL production` + redeploy. Só o valor explícito `compiler` volta para a escultura.

**Limitações registradas:** (a) o globo passa POR TRÁS da manchete durante a travessia (0.72–0.80) — é intencional, mas é o trecho mais sensível da cena; (b) a máscara de terra é editorial (≈1,4°/px), Itália/Adriático ficam grosseiros e ilhas menores que ~250 km não existem; (c) `--step-6` limita a manchete a 44 px em telas ≥ ~370 px, então o ganho de tamanho no mobile veio de encher a largura, não de subir o teto; (d) mobile só emulado (Playwright), sem aparelho físico; (e) a variante `compiler` continua no repositório mas não foi re-testada nesta passagem.

## 30/08/2026 — MILO NULL, protótipo 01 (`/lab/milo-null`)

Personagem 3D procedural (sem modelos externos) em rota isolada de laboratório — `src/components/milo/`, três + R3F 9 + drei + zustand carregados só nessa rota (`next/dynamic`, shared bundle continua 106 kB). Pipeline real de render targets numa cena só separada por layers: grid → `sceneRT`, silhueta (normais de vista + fresnel) → `maskRT`, composite que redesenha a grid procedural nas coordenadas deslocadas dentro da máscara + wireframe fragmentado + partículas. Rig de grupos aninhados, IK analítica própria de dois ossos, idle/observe/touch/dissolve procedurais, estados via zustand + GSAP (reversíveis), 3 níveis de qualidade sobre `getQuality()`, pausa fora da aba/viewport, fallback SVG (sem WebGL / reduced-motion), debug só em dev. 61 FPS na Intel UHD em todos os estados, desktop e 390 px; `tsc`/`lint`/`build` verdes. Iteração 02 (30/08): pose em três quartos, +25 % de escala, silhueta com gola/abas assimétricas, cabeça angular, linhas estruturais por zona, distorção com 6 parâmetros (`milo.config.shader`), painel interativo, debug recolhido (tecla D). Iteração 03 (30/08): massa contínua (máscara dilatada+borrada, distorção pelo gradiente da forma), wireframe −80 % com `wireframeVisibility`, densidade/sombra interna, modos DISTORTION/WIREFRAME ONLY no debug, lime só em 4 pontos; teste definitivo (wireframe=0 → figura humana pela grid) passa. Iteração 04 (30/08): lapidação com multiplicadores regionais (região por parte no alpha da máscara: pélvis −28 % de densidade, hachura −20 % no tronco/coxas, cabeça +12 % de distorção, aba +10 %), partículas por estado (full 0,65), anti-alias das bordas por fwidth. Capturas em `scripts/.rsc-audit/milo-v7-*.png` + `milo-v7-comparison.png`. Integração experimental no Hero real (30/08, commit local): feature flag `NEXT_PUBLIC_HERO_VISUAL=compiler|milo` (padrão `compiler`, idêntico ao atual) em `src/features/hero-visual/`; `src/features/milo/hero/` monta o Milo no lugar do Compiler só na variante `milo` — réplica WebGL localizada da grid DOM (12 colunas medidas por ResizeObserver), composite transparente fora da silhueta, ponte que deriva tudo do `progress` do ScrollTrigger do BuildHero (reversível), eventos `mw:visual-assemble/ready` no Boot, fallback SVG no HTML, montagem só após interação + probe + frame medido. Validação: `node scripts/.rsc-audit/hero-milo.mjs <tag> [base]`. `/lab/milo-null` não está no sitemap; `noindex`.

**Hero v3 — aprimoramento (31/08, `0dda294` + marcha `8c9e1df`, NO AR):** cards removidos de vez; composição reequilibrada (headline sobe, cabe em ~8,4 colunas via `.t-fit-hero` com `--chars`/`--chars-m`; Milo com 78 svh, para nas colunas 8–10; sub + CTA "VER PROJETOS ↓" PT/EN/ES no final); Milo mais presente no modo Hero (linhas/densidade/sombra/contorno +25–35 %, cabeça e tronco mais densos, onda curta localizada no contato); roteiro `HERO_SCENE` 0.15/0.45/0.55/0.60/0.76/0.78/0.90 (única fonte: labels da timeline; a ponte lê); interação = PUXADA de ~1,4 coluna (wdth 105→125, wght 700→900 sob a mão, `HEADLINE_AXES`), o Milo fica parado, mão e palavra derivam do mesmo objeto tweenado (`axes` + `onUpdate` → `--wdth` inline; gsap.set/fromTo em custom property sem unidade escrevia "0" → fonte clampava em 62 e o layout saltava — armadilha registrada); estágios com hierarquia (`is-active`/`is-past`); dica de scroll no rodapé técnico; overflow real corrigido (rótulo `.t-display-sm` em flex sem `min-w-0` no `#capabilities` → 467 px a 390) + `html, body { overflow-x: clip }` como guarda; reduced-motion = final estático com SVG entrando por CSS; tablet 768×1024 e 360×800 validados. Milo nunca deve ter `visibility < 1` depois de entrar (a ordem de dissolve começa pelas pernas). Refinamento final (31/08, 3ª passagem): sincronização exata — `pushProgress = (p − 0.60)/(0.76 − 0.60)` na ponte deriva peso do IK, contato, impulso da grid, onda e partículas; 0.56–0.60 é só antecipação corporal (mão ≤ 7 px); a mão em repouso já fica ao lado da extremidade (`holdOffset 0.68`); o tween dos eixos (`power2.out`) começa na mesma label `contact`; mão ativa recua atrás do quadril no final (`applyHeroBody`, `solid`) e o corpo vira +0.14 rad de frente — nada sobre "AS." nos 6 viewports. Overflow real a 360 px era o h2 do Contact CTA com `t-fit-md` (só ≥ 768) → `t-fit`; mídia do Selected Work agora escala a `img`, não a caixa clipada. `hero-sync.mjs`: sequência 0.54–0.64, reverso, salto, resize, console, mão vs "AS.", SW@360 animado, vídeos (`scripts/.rsc-audit/video/hero-{desktop-1920,mobile-390}.webm`) e folha `sync-sheet.png`. Limitação: resize 1920→1366 no meio do empurrão deixa a mão ~30 px mais baixa que um load fresco (x correto). Re-auditoria (31/08, 2ª passagem): mobile com Milo 0,72 mais à direita (não encosta na sub-headline), saída suave antes do pin soltar (grid/estágios abaixam a 0.96), armStart 0.56 (alcance curto, frase parte no toque), resize durante a cena e scroll rápido validados (`hero-resize.mjs`), console limpo. Restam: mídia do Selected Work a 360 px passa 9 px (transiente de animação, clipado, fora do Hero); mobile só emulado; a mão passiva do Milo pende sobre "AS." no estado final (texto por cima, legível).

**4ª passagem — investigação visual real (31/08, local):** o Rick analisou a versão publicada e achou uma contradição com os relatórios anteriores (que diziam ≤50 px): visualmente havia ~200 px de vão entre "PESSOAS." e Milo, o braço ativo lia como apontando para longe do texto. Confirmado que local e produção rodavam o MESMO commit (`8c9e1df`) — não era cache nem deploy desatualizado; era o número "≤50 px" mascarando uma experiência que não LIA como contato. Causas reais encontradas e corrigidas:
- **`touch.ts`**: a dobra do punho (`rightHand.rotation.x −0,4 / z +0,3`) jogava a ponta dos dedos ~20–25 px para longe da direção que o IK mirava — reduzida para −0,14/+0,1.
- **`holdOffset`** (corpo↔palavra): 0,68→0,52 (torso ficava ~1,5 coluna livre, longe demais para o braço "ler" como alcançando).
- **`stopBefore`**: 0,07→0,02 (a mão parava cedo demais).
- **`pullOffsetX/Y`**: viravam FRAÇÃO da caixa do Hero (eram px fixos — um valor fixo "acertava" numa altura de viewport e errava por 30–70 px em outra, já que o alvo do braço é lido via `panel.x/y` desprojetado pela câmera). Calibrado por varredura ao vivo: `pullOffsetX −0,007, pullOffsetY 0,06`.
- **Resultado**: mão↔extremidade caiu para ~17–19 px em 1440×900/1363×936 (dentro da meta 0–20 px), ~25–32 px em mobile/tablet, mas ainda ~35–93 px em 1920×1080 e 1366×768 (ambos 16:9) — **limitação real e documentada**: a tipografia do H1 escala por `clamp(vw)` (regra CSS própria, não normalizada por aspect ratio), enquanto a câmera/mundo 3D escala por FOV fixo; as duas não crescem juntas, então nenhuma fração única fecha a distância a <20 px em TODA largura. Corrigir de verdade exigiria ler o `font-size` computado do H1 por frame (mecanismo novo, fora do escopo de "ajuste").
- **Bug de rótulo achado de carona**: `03 MOTION` só acendia DEPOIS de Milo já ter parado de andar (o limite DESIGN/MOTION coincidia com `walkStart`, mas a caminhada usava esse MESMO ponto como início — sobra de uma versão anterior). Corrigido: nova label `design:0.10`, `walkStart:0.20` (era 0.15); agora DESIGN (grid desce) acontece ANTES da caminhada, e MOTION cobre a caminhada inteira.
- **Boot**: a headline ficava invisível por **2,1 s A MAIS** depois do overlay já ter saído (overlay saía a 2,1 s/1,5 s/0,5 s conforme primeira visita/curta/rápida, mas `headline()` só disparava a 4,2 s/2,7 s/0,9 s) — exatamente o "vazio" que o Rick descreveu. Corrigido: `headline` agora dispara junto com `exit` (2,2/1,6/0,6 s).
- **Presença do Milo**: multiplicadores de linha/densidade/sombra/contorno no shader reforçados de novo (core 0,35/0,25→0,55/0,4; densidade 0,72→0,85; sombra 0,78→0,92; contorno 1,3→1,55).
- **Diagnóstico visual (dev only, tecla H)**: pontos azul (extremidade real) e vermelho (mão ativa) recoloridos, distância em px no readout, caixa da última palavra desenhada.
- Validação: `node scripts/.rsc-audit/hero-contact-check.mjs` (6 viewports, reverso, salto, resize, overflow, console) + capturas/contact-sheet em `scripts/.rsc-audit/investigate/` (não versionado — só local). tsc/lint/build verdes, FPS 60,5. **Sem push.**

**5ª passagem — Hero MOBILE dedicado (30/08, local, sem push):** desktop intocado (mesma `HERO_SCENE`, mesmo `HEADLINE_AXES`, sem `if (small)` alterando nenhum branch existente). Timeline própria `HERO_SCENE_MOBILE` (mesmas chaves de `HERO_SCENE`, ritmo 0.15/0.42/0.56/0.70/0.90/0.96/0.97) só entra quando `small`. Mini-narrativa causal: "PESSOAS." nasce oculta por `clip-path` (headline "incompleta" desde o início), revela-se exatamente no instante do contato (0.62→0.70, sincronizado com a chegada do Milo — sem tween de wdth/wght no mobile, que fica travado no valor estável pra não arriscar overflow numa tela estreita), pausa de "garra" (0.70→0.80), depois estica como faixa elástica presa à esquerda (`transform-origin:left`, scaleX 1→1.14, skewX leve, letter-spacing, 0.80→0.90) e recupera só parcialmente (→1.06, `power3.inOut`, nunca `elastic.out`, 0.90→0.96). Corpo do Milo reage ao puxão via `pullAmount = push` (em vez de `fontWidth`, que fica zerado no mobile) nas três fórmulas de posição/yaw/lean em `MiloHeroBridge`. Milo reduzido de ~51 % para ~34 % da altura útil (`MILO_HERO.heightSvhMobile 0.34`, mesma derivação de `VIEW_H` já usada no desktop), reposicionado pra "zona de interação" entre headline e rodapé (`yMobile −0.66`, `holdOffsetMobile 0.72`); halo do composite contido só no mobile (`uHaloScale`, uniform novo, 1 = desktop inalterado, 0.55 no mobile) — o raio de amostragem do shader é fixo em UV de TELA, então numa silhueta menor ele "estourava" mais (mais evidente em retrato). Composição: headline sai do `mt-auto` (que colava tudo no rodapé com um vão enorme acima, entre o código decorativo e a manchete) pra `mt-6` fixo; rodapé técnico (`SHIP`) passa a usar `mt-auto` pra continuar ancorado embaixo; bloco ENTREGA (sub + CTA) vira `absolute` no mobile — antes reservava altura mesmo invisível (`autoAlpha:0` não recolhe layout), e a 320×568 código+headline+ENTREGA+rodapé em fluxo normal não cabiam (rodapé cortado ~31 px abaixo da dobra); o próprio rodapé técnico (`data-ship`) ganha fade-out no instante `outro`, só no mobile, porque sem isso colide com o CTA "VER PROJETOS ↓" na tela mais baixa testada. Fallback SVG (`MiloHeroFallback`) reposicionado só no mobile (`top-[40%] h-[32%]`, era `top-[22%] h-[44%]`) pra bater com a composição nova. Testado num harness descartável (iframe same-origin em `/mobile-harness.html`, apagado ao final — precisou soltar `frame-ancestors 'self'` temporariamente só em dev no `csp.ts`, revertido antes de terminar) em 320×568/390×844/430×932: sem overflow, sem Milo↔rodapé colidindo, sem erro/warning novo no console. **Limitação de teste registrada**: a janela do Chrome desta automação roda sem foco real de SO (`document.hidden` fica `true` a maior parte do tempo), o que congela o ticker do GSAP entre ações — contornado com navegação fresca + tempo de assentamento antes de cada leitura; verificação contínua frame-a-frame (arrastar a barra ao vivo) não foi possível, só checkpoints discretos de progresso. tsc/lint verdes (só o warning pré-existente de `<img>` no OG image). **Sem push/deploy.**

**Cena cinematográfica do Hero (31/08, local — versão anterior, arrasto de ré):** o Hero virou uma cena — o Milo entra ANDANDO pela direita (totalmente fora da tela em p=0), para, desce o braço até a extremidade de "PESSOAS.", segura e ARRASTA a palavra para a direita andando de ré enquanto a Archivo expande (wdth 62→125); impacto (recuo, impulso na grid, presença mais sólida) e composição final parada. Cards flutuantes de projetos removidos do Hero (Kavita/Terral/Vertex) com tudo que só existia por eles. Roteiro em `HERO_SCENE` (`build-hero.tsx`): design 0.08 · walkStart 0.20 · walkEnd/armStart 0.57 · contact 0.68 · pullEnd 0.84 · impactEnd 0.92 — timeline com duração exatamente 1 (labels == progresso), a ponte (`MiloHeroBridge`) lê as labels da própria timeline. Caminhada procedural por DISTÂNCIA (`animation/walk.ts`: fase = deslocamento/passo, apoio linear → sem deslizamento; passo ajustado para fechar em ciclos inteiros). Mão/palavra/corpo derivam do mesmo `--wdth` (fromTo — com `invalidateOnRefresh` o `to` relia o início como 0 e a fonte clampava em 62: a expansão "pulava"). Headline em spans por palavra (`data-word`; no mobile cada palavra é uma linha — layout estável durante a expansão), `aria-label` completo no h1; a mão ancora em `[data-headline-word=last]` medido por `Range` por frame. O canvas agora é PORTALADO para dentro da `#top` (a section pinada é `position:fixed` = contexto de empilhamento; canvas fixo fora dela ficava por cima de tudo, inclusive do SHIP). Mobile/touch: mesma cena com pin `+=220%`, canvas em qualidade `low` (política de montagem abriu para coarse). Reduced-motion: composição final estática, SVG em cena. Validação: `node scripts/.rsc-audit/hero-scene.mjs [base]` (4 viewports: sequência, reverso, FPS, overflow) + `hero-reduced.mjs` + `hero-frames.mjs`; capturas `scripts/.rsc-audit/final-*.png`, `scene-*.png`, `scene-m-*.png`. Decisão de direção: como a 2ª linha expandida ocupa ~82 % da largura, nenhum braço parado acompanha a extremidade — por isso o Milo arrasta andando de ré e termina na direita segurando o fim da frase (não "centro-direita" literal do briefing).

**Coreografia da headline (30/08, `3dee38e`, no ar — SUBSTITUÍDA pela cena acima, ainda não publicada):** o braço só trabalha durante a transformação tipográfica (wdth 62 % → 125 %) — labels `typeAnticipate`/`typeReach`/`typeContact`/`typePullEnd`/`typeRelease`/`typeSettle` na mesma timeline do BuildHero (contato→pullEnd = o tween de wdth, 0,38 + 0,18; normalizado 0,365 → 0,538). Âncora medida no DOM real (`Range` sobre `[data-headline-line=primary]`, `.right`), remedida a cada refresh/frame; `pullProgress` = `(--wdth − 62)/63` lido do `h1`. Braço em descanso em imagens/scan/ship; sem interação com cursor; tick lime na palma (`uContact`); transferência de peso no tronco. Tudo função pura do `progress` → reversível (validado: mesma posição ida/volta, 60 FPS em 1920/1440/1366). Debug dev-only na tecla **H**. Validação: `node scripts/.rsc-audit/hero-gesture.mjs [base]`; capturas `scripts/.rsc-audit/milo-type-*.png` + `milo-type-sequence.png`. **Limitação aceita:** com o Milo em cx 0,69 a ponta condensada de "CÓDIGO DEVE" fica ~300 px fora do alcance no contato; a mão aponta e só encontra a linha perto da expansão total (~110 px acima). Fechar de vez = mover/escalar o Milo ou ancorar na linha 2 — decisão pendente do Rick. Produção mostra o Milo só em ponteiro fino ≥ 720 px sem reduced-motion e com GPU aprovada; caso contrário fica o SVG (`MiloHeroFallback`). Voltar ao Compiler: `vercel env rm NEXT_PUBLIC_HERO_VISUAL production` + redeploy.

## 29/08/2026 — bug de navegação ("RSC bruto") investigado e corrigido

Relatório completo em `docs/rsc-bug-investigation.md`. Não era RSC/middleware/View Transitions: o Boot removia do DOM (`el.remove()`) um nó renderizado pelo React e a navegação seguinte saindo da Home caía em `NotFoundError: removeChild` → página de erro global do Next. Reproduzido em Chromium e WebKit, local e produção; corrigido em `boot-controller.tsx` (esconder, não remover). De carona: `ViewTransitions` sem `stopPropagation` (o cookie de idioma voltou a ser gravado) e `LanguageSwitch` com `prefetch={false}` (o prefetch de "/" seguia o redirect do cookie e prendia o clique em PT no inglês). Teste automatizado: `node scripts/rsc-navigation-audit.mjs --base http://127.0.0.1:3000` (`--only dwell` reproduz o crash antigo). Deploy `334d541` no ar e validado em produção (sondas dwell, lang-swap e cookie).

## Onde paramos (28/08/2026, fim do dia)

Quatro entregas grandes fecharam hoje, todas no ar e verificadas em produção:

1. **Fase 03/04** — Selected Work em mundos, cases cinematográficos, polimento cross-browser (`docs/rebuild/04-report.md`).
2. **Trilíngue PT/EN/ES** — rotas localizadas, dicionários tipados, 404 por idioma, SEO internacional (`docs/rebuild/05-i18n.md`).
3. **"WE COMPILE IMPOSSIBLE REALITIES"** — escultura procedural THE COMPILER, leis por mundo, transições por case, Lab com 3 experimentos, som opcional (`docs/rebuild/06-compile.md`).
4. **SplitText sob demanda** — fora do bundle inicial + correção do repouso da física do Break.

## Progresso (checklist `site-institucional`)

```
Fase 01 — Briefing              ✓  conceito "the website that builds itself" + "we compile impossible realities"
Fase 02 — Arquitetura           ✓  trilíngue por tabela (src/i18n/routing.ts); URLs antigas 308 direto; 05-i18n.md
Fase 03 — UX                    ✓  home em 9 atos; HOLD TO INSPECT; Break/Rebuild; mobile com versão própria
Fase 04 — UI                    ✓  tokens em styles/tokens.css; Archivo variável (eixo wdth) + JetBrains Mono; grid 12/8/4
Fase 05 — Conteúdo              ✓  PT/EN/ES completos e tipados; espanhol revisado por leitura completa em 05/09 (neutro LatAm, 1 ajuste), sem falante nativo
Fase 06 — Frontend              ✓  404/500 com identidade, build verde, sem componentes legados
Fase 07 — Motion Design         ✓  escultura com 8 estados, leis por mundo, 5 transições de rota, 3 experimentos no Lab
Fase 08 — SEO On-Page           ✓  canonical/hreflang/x-default, og:locale, sitemap 111 URLs em 3 idiomas, JSON-LD inLanguage
Fase 09 — Performance           ✓  mobile 86 · desktop 96 · a11y 100 · JS inicial 246,2 KB gzip (orçamento em perf-budget.md)
Fase 10 — Acessibilidade        ✓  a11y 100; teclado, foco visível, reduced-motion, conteúdo sem canvas, controle de som com aria-pressed
Fase 11 — Analytics e conversão ◐  whatsapp_click + email_click no código; Web Analytics NUNCA FOI ATIVADO no painel da Vercel (05/09) — ativar e confirmar eventos
Fase 12 — Segurança             ✓  CSP estático, headers e redirects; strict mode
Fase 13 — QA                    ◐  Chromium/WebKit automatizados OK (+ auditoria de navegação/RSC 29/08: 1285 passos Chromium, 0 falhas); Safari e Firefox FÍSICOS pendentes
Fase 14 — Deploy                ✓  Vercel READY; rotas, redirects e 404 conferidos em produção
Fase 15 — Indexação             ◐  propriedade sc-domain:milweb.com.br verificada; sitemap NUNCA foi enviado (lista vazia em 05/09) — enviar /sitemap.xml e pedir indexação
Fase 16 — Entrega               ◐  docs/entrega.md escrito 05/09; falta deploy, tag v1.0.0 e os dois cliques (Analytics, sitemap)
```

## Métricas atuais (Lighthouse 13, build de produção local, máquina sem carga)

| Página | Perf | LCP | TBT | CLS | a11y |
|---|---|---|---|---|---|
| Home mobile | **86** (83/85/90) | 2,4 s | ~350 ms | 0,08 | 100 |
| Home desktop | **96** | 0,6 s | 20 ms | 0,005 | 100 |
| Case (Kavita) mobile | **92** | 3,2 s | 150 ms | 0,035 | 100 |
| /lab mobile | **91** | 3,3 s | 140 ms | — | 100 |
| /contato mobile | **90** | 2,8 s | 240 ms | — | 100 |

JS inicial da home: **246,2 KB gzip** (limite documentado: 250).

## Como validar (scripts prontos)

```bash
pnpm build && MW_LOCAL_HTTP=1 pnpm start -p 3005   # feche navegadores antes de medir
node scripts/i18n-audit.mjs http://localhost:3005   # vazamento de idioma + SEO (114 rotas)
node scripts/i18n-visual.mjs http://localhost:3005  # 4 viewports × 3 idiomas, seletor, menu
node .audit/qa-phase7.mjs                           # transições, reduced-motion, sem WebGL, teclado
node .audit/split-test.mjs                          # headline, Break e gravidade do Lab
bash .audit/routes.sh                               # rotas 200 · legadas 308 · cookie · 404
```

Os scripts em `.audit/` são locais (ignorados pelo git); os de `scripts/` estão versionados.

## Investigação aberta (31/08 → 01/09): travamento total relatado pelo Rick
Sintoma: site congela inteiro (todas as animações, hero ao fim; screenshot em produção parado no painel Terral em estado fantasma) e só volta ao clicar numa língua (remonta o layout [lang] → ScrollProvider/Lenis novos). NÃO reproduzido por script apesar de: trocas de língua (topo/meio/fundo, rápidas), varreduras de wheel na página inteira (dev e prod), timings de boot/Esc variados, case → back (bfcache), forward/back, aba oculta, resize. Único congelamento local coincidiu com hot-reload do dev (artefato). Assinatura quando travado: wheel engolido sem movimento, `lenis-scrolling` preso, rAF vivo, scroll nativo ok. Guards embarcados (commit local): failsafe do Boot fora do GSAP (o `html.booting{overflow:hidden}` não pode ficar preso), watchdog do Lenis no ScrollProvider (isScrolling sem deslocamento por 3 s → stop/start + refresh; `pageshow` também recompõe) e CTA "VER PROJETOS ↓" corrigido (era `/#work`, não interceptado — clique não fazia nada; agora `#work` via Lenis). Hipótese não descartada: aba do Edge instrumentada pela extensão Claude-in-Chrome congela a ~1 FPS (memória do projeto).

**01/09 — reprodução confirmada pelo Rick + 2 causas reais encontradas por leitura de código (Edge, desktop):** gatilho confirmado — "fecho a aba e entro no site de novo" (não troca de aba, fecha e reabre — Ctrl+Shift+T ou histórico). Duas lacunas reais achadas, ambas corrigidas e no ar:
1. **`scroll-provider.tsx`**: o watchdog do Lenis só recompunha em `pageshow` (bfcache/navegação) — trocar de aba e voltar (o caso mais comum de aba oculta) não dispara `pageshow`, só `visibilitychange`, que não existia em lugar nenhum do código. Enquanto a aba fica oculta o rAF do `gsap.ticker` pausa (comportamento do próprio browser, confirmado ao vivo nesta sessão testando viewport mobile: `document.hidden` trava o ticker indefinidamente até a aba voltar a ficar visível) e o watchdog não adianta nada enquanto ele não voltar a rodar. Agora `onShow` (o mesmo handler do `pageshow`) também roda em `visibilitychange` quando `document.visibilityState === "visible"`.
2. **`boot.tsx`** (`SKIP_SCRIPT`, inline, roda antes da hidratação): `history.scrollRestoration` nunca era setado, então ficava no padrão `'auto'` do browser — ao reabrir uma aba fechada (Ctrl+Shift+T no Edge/Chrome restaura sessionStorage junto), o browser reaplica o scrollY salvo por conta própria, mas o Lenis (import assíncrono em `ScrollProvider`) inicializa DEPOIS achando que a página está em 0 — a posição nativa já pulou pra Y>0 enquanto a interna do Lenis fica presa em 0, e o wheel passa a mover algo que não corresponde a nada visível (exatamente "wheel engolido, nada anda" da assinatura registrada acima). Corrigido: `history.scrollRestoration='manual'` logo no início do `SKIP_SCRIPT`, antes de qualquer outra coisa — a página sempre começa em 0 de verdade, sem a corrida.
- Nenhuma correção foi testada com repro real (não reproduzido por script, como o resto desta investigação) — são lacunas genuínas achadas por leitura de código, consistentes com o gatilho que o Rick confirmou, de baixo risco (puramente aditivas, não mudam nada visual). **Pendente**: Rick confirmar se resolveu de fato depois do deploy, testando especificamente "fechar aba (Ctrl+W) → reabrir (Ctrl+Shift+T)" no Edge com a página scrollada.
- tsc/lint verdes. Push + deploy autorizados pelo Rick ("sim, sobe agora").

**01/09 — 2ª causa real, confirmada com dados ao vivo (console, snippet rodado pelo Rick a pedido):** o travamento visual não era só o wheel — o Rick mandou prints comparando "como deveria ser" vs "como está ficando" nos 4 cases do Selected Work (`src/sections/home/selected-work.tsx`). `getComputedStyle` confirmou: Kavita/Terral/Vertex ficam com `textOpacity: '0.15'` (o valor final da timeline de SAÍDA) sempre — mesmo parado, sem rolar — enquanto só Aurex (o último case, sem timeline de saída) mostra `'1'` de verdade. Causa: a timeline de saída de cada case usa o **próximo** (`trigger: next`) como referência de scroll — como todo `<article data-panel>` é `position: sticky` (não `pin:true`), medir a posição de um elemento sticky que ainda não "grudou" é instável nesse padrão de cases empilhados; o range da saída sai medido errado e ela fica sempre "completa". Corrigido: trigger trocado pra `panel` (o próprio, cuja entrada já prova ser confiável) com posições equivalentes (`bottom bottom` → `bottom 15%`, mesmo timing já que os painéis são contíguos). tsc/lint verdes. **Sem push ainda** — o Rick pediu pra pausar ("vou tentar outra coisa, deixa o PC esfriar") antes de eu subir ou pedir novo teste. Retomar perguntando se quer testar local primeiro ou já subir.

## Bloqueios / armadilhas (ler antes de mexer)

- **A escultura só monta na primeira interação e só com GPU acelerada.** É decisão de performance, não bug: pagar a compilação do shader no carregamento levava o TBT mobile de 290 ms para 9.850 ms. Até lá vale a silhueta SVG. Ver `docs/rebuild/06-compile.md` §9.
- **404 por idioma**: `notFound()` sob o layout raiz dinâmico `[lang]` só manda o shell genérico, e a Vercel intercepta rewrite com status 404. Por isso a rota desconhecida é decidida no middleware → `app/not-found.tsx` lê `x-milweb-locale`. Ver `05-i18n.md`.
- **Medição de performance**: cada execução do Lighthouse deixa processos do Edge vivos; sem matá-los, a nota cai até 20 pontos. `taskkill //F //IM msedge.exe` entre execuções.
- **Firefox**: o build do Playwright não abre página neste Windows; sem Firefox físico. Safari físico também indisponível — a tipografia variável precisa de olho humano lá.
- **OG image dos cases** não renderiza em `next dev` no Windows (bug do @vercel/og); em produção está validada.
- **`/diagnostico`** e seus componentes (dependency-calc, google-sim, fair-price) ainda usam o shim CSS plano — funcionam, mas destoam do sistema novo.
- **Vídeos `public/lab/full-*.mp4` (67 MB)** continuam no repo; não entram no primeiro acesso, mas pesam no clone.
- **`pnpm build` e `next dev` não podem dividir a mesma `.next` ao mesmo tempo**: o build sobrescreve os chunks do dev server (erro "Cannot find module './vendor-chunks/gsap@3.15.0.js'"). Pare o dev, ou rode o build, e depois `rm -rf .next` + subir de novo.
- **`NEXT_PUBLIC_HERO_VISUAL` é inlined no build**: trocar a variante exige reiniciar o dev server / redeploy. Em produção está `milo`.

## Próxima ação (em ordem)

0. **Rick, 3 cliques**: Vercel → milweb → Analytics → **Enable**; Search Console → Sitemaps → enviar `https://milweb.com.br/sitemap.xml`; autorizar `npx vercel --prod` (a11y 100 + email_click + "cada centavo" estão só na main).
1. Depois do deploy: `vercel inspect milweb.com.br` (domínio no deploy novo), Lighthouse desktop (a11y deve voltar a 100), `git tag -a v1.0.0` + push da tag, marcar Fase 16 ✓.
2. Confirmar no painel Analytics → Events que `whatsapp_click`/`email_click` chegam (clicar num CTA em produção) → Fase 11 ✓.
3. Search Console: acompanhar a leitura do sitemap (114 URLs) e pedir indexação de `/`, `/en`, `/es` → Fase 15 ✓.
4. Remedir Lighthouse mobile em máquina limpa (fechar Edge, matar processos) e comparar com o 86 local; se o 66–68 se confirmar, investigar o chunk 3995 (0,8 s de script) em `perf-budget.md`.
5. Fase 13: Safari/Firefox físicos quando houver aparelho (ou marcar N/A com justificativa).
6. Reskin do `/diagnostico` no sistema novo (última página com cara antiga).

## Notas de N/A

- (nenhuma até aqui — nenhuma fase foi dispensada)
